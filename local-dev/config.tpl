current: ${CURRENT}
default: ${DEFAULT}
lagoons:
  ${LAGOON_NAME}:
    graphql: ${GRAPHQL}
    hostname: ${HOSTNAME}
    kibana: ${KIBANA}
    port: ${PORT}
    sshkey: ${SSHKEY}
    token: ${TOKEN}
    ui: ${UI}
    version: ${VERSION}
