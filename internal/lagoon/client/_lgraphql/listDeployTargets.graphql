query {
  listDeployTargets: allKubernetes{
    id
    name
    created
    consoleUrl
    cloudRegion
    cloudProvider
    friendlyName
    token
    sshHost
    sshPort
    routerPattern
    monitoringConfig
  }
}