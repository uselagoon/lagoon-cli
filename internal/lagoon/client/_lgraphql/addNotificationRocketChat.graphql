mutation (
  $name: String!,
  $channel: String!,
  $webhook: String!) {
    addNotificationRocketChat(input: {
      name: $name,
      channel: $channel,
      webhook: $webhook
    }) {
      id
      name
    }
  }
